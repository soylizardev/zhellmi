[package]
name = "zhellmi"
version = "0.9.0"
edition = "2024"

[profile.dev]
opt-level = 1               # Use slightly better optimizations.


[dependencies]

libc = "0.2.183"
rustyline = { version = "18.0.0", features = ["derive"]}
